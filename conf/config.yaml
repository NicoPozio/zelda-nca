# Config principale: compone i gruppi e tiene i parametri trasversali.
defaults:
  - model: nca
  - data: zelda
  - train: default
  - _self_

seed: 42
device: auto        # 'auto' -> cuda se disponibile, altrimenti cpu